schema: http://localhost:4000/graphql
documents: src/**/*.ts
generates:
  src/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      # strict: trueと型が合わない
      # - urql-svelte-operations-store
