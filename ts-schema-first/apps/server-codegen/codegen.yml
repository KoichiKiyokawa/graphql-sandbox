schema: ../../schemas/**/*.gql
generates:
  ./src/__generated.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      scalars:
        DateTime: Date
      contextType: ./context#GraphQLContext
      useIndexSignature: true
      defaultMapper: Partial<{T}>
